<UserControl x:Class="POESKillTree.Views.Equipment.StashView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:equipment="clr-namespace:POESKillTree.ViewModels.Equipment"
             mc:Ignorable="d" 
             d:DesignHeight="300"
             d:DataContext="{d:DesignInstance equipment:StashViewModel}">
    <UserControl.Resources>
        <CompositeCollection x:Key="ScrollBarOverlayCollection">
            <CollectionContainer Collection="{Binding Bookmarks}" />
            <CollectionContainer Collection="{Binding SearchMatches}" />
        </CompositeCollection>
    </UserControl.Resources>
    <Grid>
            
    </Grid>
</UserControl>
