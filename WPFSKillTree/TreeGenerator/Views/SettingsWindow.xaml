<controls:MetroWindow 
    x:Class="POESKillTree.TreeGenerator.Views.SettingsWindow"
    xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:POESKillTree.TreeGenerator.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
    xmlns:vm="clr-namespace:POESKillTree.TreeGenerator.ViewModels"
    d:DataContext="{d:DesignInstance vm:SettingsViewModel}"
    Title="{Binding DisplayName}" Height="550"
    WindowStartupLocation="CenterOwner" ShowInTaskbar="False" ResizeMode="NoResize" SizeToContent="Width">
    <Grid Background="{DynamicResource WhiteColorBrush}">
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.Resources>
            <DataTemplate DataType="{x:Type ValidationError}">
                <TextBlock 
                    FontStyle="Italic"
                    Foreground="Red"
                    HorizontalAlignment="Right"
                    Text="{Binding Path=ErrorContent}" 
                    />
            </DataTemplate>
        </Grid.Resources>

        <Label Grid.Row="0" Grid.Column="0" Content="_Level:" Margin="5" Target="{Binding ElementName=Level}"/>
        <controls:NumericUpDown Grid.Row="0" Grid.Column="1" x:Name="Level" Minimum="1" Maximum="100"
                                Value="{Binding Path=Level, ValidatesOnDataErrors=True}"/>

        <Label Grid.Row="1" Grid.Column="0" Content="_Additional points:" Margin="5" Target="{Binding ElementName=AddPoints}"/>
        <controls:NumericUpDown Grid.Row="1" Grid.Column="1" x:Name="AddPoints" Minimum="0" Maximum="24"
                                Value="{Binding AdditionalPoints, ValidatesOnDataErrors=True}"/>

        <Label Grid.Row="2" Grid.Column="0" Content="_Total points:" Margin="5" Target="{Binding ElementName=TotalPoints}"/>
        <controls:NumericUpDown Grid.Row="2" Grid.Column="1" x:Name="TotalPoints" Minimum="0" Maximum="123"
                                Value="{Binding TotalPoints, ValidatesOnDataErrors=True}"/>

        <CheckBox Grid.Row="0" Grid.Column="2" Margin="20,5,10,5" Content="include Check-tagged nodes" IsChecked="{Binding IncludeChecked}"/>
        <CheckBox Grid.Row="1" Grid.Column="2" Margin="20,5,10,5" Content="exclude Cross-tagged nodes" IsChecked="{Binding ExcludeCrossed}"/>
        <CheckBox Grid.Row="2" Grid.Column="2" Margin="20,5,10,5" Content="include imported items" IsChecked="{Binding ImportItems}"/>
        <CheckBox Grid.Row="0" Grid.Column="3" Margin="10,5" Content="only search in current tree" IsChecked="{Binding TreeAsSubset}"/>
        <CheckBox Grid.Row="1" Grid.Column="3" Margin="10,5" Content="current tree as start point" IsChecked="{Binding TreeAsStart}"/>

        <Button Grid.Row="2" Grid.Column="3" Content="_Run" Margin="10,5" Command="{Binding RunCommand}" IsDefault="True"/>
        
        <TabControl Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="4" SelectedIndex="{Binding SelectedTabIndex}">
            <TabItem DataContext="{Binding Tabs[0]}" Header="{Binding DisplayName}">
                <local:SteinerGeneratorTab/>
            </TabItem>
            <TabItem DataContext="{Binding Tabs[1]}" Header="{Binding DisplayName}">
                <local:AutomatedGeneratorTab/>
            </TabItem>
            <TabItem DataContext="{Binding Tabs[2]}" Header="{Binding DisplayName}">
                <local:AdvancedGeneratorTab/>
            </TabItem>
        </TabControl>

    </Grid>
</controls:MetroWindow>
